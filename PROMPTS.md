# PROMPTS.md

## Model Choice
Prompt:
"Suggest the best TensorFlow.js model for real‑time object detection on mobile in an Ionic/Vue app. I need on‑device inference and a lightweight model."

Result Summary:
Coco‑SSD chosen for speed, availability, and ease of use.

## Camera Integration
Prompt:
"Write Ionic/Vue code to get camera stream with getUserMedia in a Capacitor WebView and bind it to a video element."

## TFJS + Coco‑SSD Integration
Prompt:
"Provide Vue 3 component code that loads tfjs + coco‑ssd, waits for model, and runs model.detect(video) in a loop."

## Overlay Rendering
Prompt:
"Show how to draw bounding boxes + labels on a canvas overlay on top of the video element."

## Performance
Prompt:
"How can I throttle detection to 10–20 FPS and avoid UI blocking in a requestAnimationFrame loop?"

## Build Android APK
Prompt:
"Give the exact steps to build an APK with Ionic + Capacitor and ensure camera permission is configured."

## Notes
Add screenshots of the running app and the terminal output when you build the APK.

