# T-800 Vision Lite - TASKS

1. Create Ionic Vue project and run once in browser.
2. Add Capacitor and verify Android platform setup.
3. Implement camera stream with `getUserMedia` and `<video>`.
4. Add TFJS + Coco‑SSD, load model, show “Ready” state.
5. Implement inference loop with throttle and FPS counter.
6. Draw bounding boxes and labels on canvas overlay.
7. Build HUD panel for target object + confidence + status.
8. Optimize performance (lower resolution / skip frames).
9. Build Android APK and test on device.
10. Capture screenshots and fill `PROMPTS.md`.

